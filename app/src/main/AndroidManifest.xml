<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="onlyid.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="唯ID"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".user_info.EditActivity" />
        <activity android:name=".user_info.AvatarActivity" />
        <activity android:name=".app_manage.AppsActivity" />
        <activity android:name=".device_manage.DevicesActivity" />
        <activity android:name=".scan_login.ScanActivity" />
        <activity
            android:name=".user_info.UserActivity"
            android:label="个人信息" />
        <activity
            android:name=".LoginActivity"
            android:label="登录" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>